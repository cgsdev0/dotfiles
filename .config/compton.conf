backend = "glx";
vsync = "opengl-swc";

fading = true;
fade-delta = 3;
fade-in-step = 0.03;
fade-out-step = 0.03;

opacity-rule = [
"70:class_g = 'St' && focused",
"55:class_g = 'St' && !focused"
]

blur: {
    method = "dual_kawase";
    strength = 4;
}

shadow-exclude = [
  "class_g = 'Polybar'"
]
