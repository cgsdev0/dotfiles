#!/bin/bash
if [ "$1" != "" ]; then
    git reset $@
else
    cd $(git rev-parse --show-toplevel)
    git diff --name-only --cached | fzf --height=40% --prompt="git reset: " -m | xargs -r git reset -q
fi

