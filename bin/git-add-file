#!/bin/bash
if [ "$1" != "" ]; then
    git add $@
else
    cd $(git rev-parse --show-toplevel)
    git ls-files . -m -o --exclude-standard | fzf --height=40% --prompt="git add: " -m | xargs -r git add
fi

